# PostgreSQL connection.
# On Docker Compose the host is "db"; locally use "localhost".
DATABASE_URL="postgresql://ovgs:ovgs@localhost:5432/ovgs?schema=public"

# API port
PORT=3000

# Log level (trace|debug|info|warn|error)
LOG_LEVEL=info
